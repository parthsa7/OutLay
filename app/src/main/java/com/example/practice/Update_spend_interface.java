package com.example.practice;

public interface Update_spend_interface {
    public void Update_Spend_database(SpendDatabase spendDatabase);
    public void Delete_Spend_database(SpendDatabase spendDatabase);
}
